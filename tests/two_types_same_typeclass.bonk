// Mode: types
// Prelude: false
// Description: Two types, same typeclass

// Types should unify to the same predicate

class Show =
    | show : this -> string

let a = [x] [y] (show x + show y, x - y) in