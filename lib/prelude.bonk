// --- Option module ---
sum Option 'a =
    | Some 'a
    | None unit
in

// --- List module ---
sum List 'a =
    | Cons 'a * List 'a
    | Nil unit
in

let tail = [lst]
    match lst with
    | Cons (h, t) -> Some t
    | Nil _       -> None ()
in

()